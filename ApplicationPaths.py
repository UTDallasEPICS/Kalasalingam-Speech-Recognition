ApplicationPaths = {
    # We NEED to make is so that the user can update this and also make it so that we have multiple names for the same path.
    "NotePad" : "C:/WINDOWS/system32/Notepad.exe", # windows global path for Notepad
    
}
